pipeline {
    agent any

    environment {
        APP_NAME = "Amazon"
        BUILD_ENV = "DEV"
    }

    stages {

        stage('Print') {
            steps {
                echo "${APP_NAME}"
                echo "${BUILD_ENV}"
            }
        }
    }
}

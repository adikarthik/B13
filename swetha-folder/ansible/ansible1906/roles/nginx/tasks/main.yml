
# tasks file for roles/nginx
---
- name: Install nginx
  package:
    name: nginx
    state: present

- name: Verify nginx
  command: nginx -v
  register: nginx_version

- debug:
    var: nginx_version.stderr

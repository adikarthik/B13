pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                git 'https://github.com/sanjugm/Devops_Proj.git'
            }
        }

        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }

        stage('Deploy') {
            steps {
                echo "Application Deployed Successfully"
            }
        }
    }
}

apiVersion: v1
kind: Service
metadata:
  name: demo
  labels:
    app: demo
spec:
  type: NodePort
  selector:
    name: test
  ports:
    - port: 9090
      targetPort: 80
      nodePort: 31111
      protocol: TCP

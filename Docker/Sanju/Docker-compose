version: "3.9"

services:

  amazon:
    build: ./amazon
    container_name: amazon-app
    ports:
      - "8080:8080"
    restart: always

  jarapp:
    build: ./jar-app
    container_name: jar-app
    ports:
      - "8081:8081"
    restart: always

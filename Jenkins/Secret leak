pipeline { 
    agent any 
 
    stages { 
        stage('Read Secret') { 
            steps { 
                withCredentials([ 
                    string(credentialsId: 'demo-secret', variable: 'MY_SECRET') 
                ]) { 
 
                    echo "Credential loaded." 
                    echo "Secret is: ${MY_SECRET}" 
                } 
            } 
        } 
    } 
}

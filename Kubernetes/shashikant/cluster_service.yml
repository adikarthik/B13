apiVersion: v1
kind: Service

metadata:
  name: tomcat
  labels: 
   app: tomcat

spec:
  type: ClusterIP

  selector:
    app: tomcat

  ports:
  - name: 8080-8080
    protocol: TCP
    port: 8080
    targetPort: 8080
